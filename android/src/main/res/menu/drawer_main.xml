<?xml version="1.0" encoding="utf-8"?>
<menu xmlns:android="http://schemas.android.com/apk/res/android">
    <group android:checkableBehavior="single">
        <item
            android:id="@+id/tracks"
            android:icon="@drawable/ic_library_books"
            android:title="@string/tracks_btn" />
        <item
            android:id="@+id/playlists"
            android:icon="@drawable/ic_queue_music"
            android:title="@string/playlists_btn" />
        <item
            android:id="@+id/settings"
            android:icon="@drawable/ic_settings_icon"
            android:title="@string/settings_btn" />
    </group>
</menu>